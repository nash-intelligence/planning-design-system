name: nash-planning-design-system

services:
  storybook:
    build:
      context: .
      target: dev
    environment:
      CHOKIDAR_USEPOLLING: "true"
    ports:
      - "${NASH_STORYBOOK_PORT:-6006}:6006"
    volumes:
      - ./:/workspace
      - /workspace/node_modules
